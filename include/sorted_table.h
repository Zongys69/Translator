#include <iostream>
#include <utility>
#include "List.h"
template <class TKey, class TValue>

class SortedTable {
	std::vector<std::pair<TKey, TValue>> data;
public:
	iterator find((const TKey& key, const TValue& value) {

	}
	iterator errase(const TKey& key, const TValue& value){
		}
	iterator insert(const TKey& key, const TValue& value){
	}
};